# Hanzo CI/CD for hanzoai/vault: .github/workflows/cicd.yml imports hanzoai/ci,
# which reads this file. The build door builds each image, one BuildKit Job
# per platform.
images:
  - name: vault
    context: .
    dockerfile: Dockerfile
    repo: ghcr.io/hanzoai/vault
    platforms: [linux/amd64]
