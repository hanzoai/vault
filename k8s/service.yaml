apiVersion: v1
kind: Service
metadata:
  name: vault-service
  namespace: hanzo-vault
  labels:
    app: vault-service
spec:
  type: ClusterIP
  ports:
    - port: 8443
      targetPort: 8443
      protocol: TCP
      name: mtls
  selector:
    app: vault-service
